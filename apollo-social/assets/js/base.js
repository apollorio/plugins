/**
 * Apollo Base JS (Local Fallback)
 * 
 * This is a fallback version when CDN (https://assets.apollo.rio.br/base.js) is unavailable.
 * Main source: https://assets.apollo.rio.br/base.js (work in progress)
 * 
 * @package Apollo_Social
 */

(function() {
	'use strict';

	// Mark as loaded.
	window.apolloBaseLoaded = true;

	// Placeholder for Apollo base functionality.
	// This will be replaced by the full version from CDN when available.
	
	console.log('[Apollo Base] Local fallback loaded. CDN version preferred: https://assets.apollo.rio.br/base.js');
})();

